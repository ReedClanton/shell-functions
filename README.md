# shell-functions
This contains a number of shell functions that may be of use. The intention is for the root of this repository to be cloned to an area in a user's home directory. Then sourceFunctions.sh should be soruced by the user's shell configuration.
